# Local dev using Python venv

Ensure you're in the /part1/log-output/web directory. Create the Python virtual environment, activate it and install the requirements (locally).

```shell
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
